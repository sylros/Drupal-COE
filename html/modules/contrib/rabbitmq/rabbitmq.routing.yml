rabbitmq.properties:
  path: '/admin/reports/status/rabbitmq'
  defaults:
    _controller: '\Drupal\rabbitmq\Controller\ServerPropertiesController::build'
    _title: 'RabbitMQ properties'
  requirements:
    _permission: 'administer site configuration'
